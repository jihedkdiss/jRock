# jRock
jRock is a Rock, Paper, Scissors console game coded in Java.

## How to use
1. Make sure you have JDK to compile it.
2. Run `javac Main.java` at the `jrock` folder.
3. Run `java jrock.Main` at the `src` folder.
4. Enjoy playing against the world most advanced AI system ever.
